-- ~/.config/nvim/lua/plugins/commentary.lua
return {
  "tpope/vim-commentary",
  config = function()
    -- No specific configuration needed as vim-commentary works out-of-the-box
    -- You can add key mappings or additional settings if necessary.
  end,
}

